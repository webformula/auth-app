{
  "mount": {
    "test-app": "/test",
    "app": "/"
  },
  "plugins": [
    [
      "@snowpack/plugin-run-script",
      {
        "cmd": "node --experimental-modules buildEntry.mjs",
        "watch": "nodemon -x $1"
      }
    ]
  ]
}
